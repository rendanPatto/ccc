---
description: Compatibility entry for the renamed /pickup command. Prefer /pickup target.com.
---

# /resume

`/resume` remains available as a compatibility entry.

Use `/pickup target.com` for the primary command going forward.

## Usage

```text
/pickup target.com
```

## Notes

- `/resume` maps to the same continue-hunt flow.
- New docs and examples should use `/pickup`.
